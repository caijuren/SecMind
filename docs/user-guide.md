# SecMind AI 安全运营平台 — 产品使用指南

> **文档版本**：v2.0.0 | **适用版本**：SecMind v2026.05 | **更新日期**：2026-05-12

---

## 目录

- [第一章 产品简介与快速开始](#第一章-产品简介与快速开始)
  - [1.1 产品概述](#11-产品概述)
  - [1.2 技术架构](#12-技术架构)
  - [1.3 安装部署](#13-安装部署)
  - [1.4 首次登录与引导流程](#14-首次登录与引导流程)
- [第二章 界面导航说明](#第二章-界面导航说明)
  - [2.1 整体布局](#21-整体布局)
  - [2.2 侧边栏五大功能分组](#22-侧边栏五大功能分组)
  - [2.3 公开页面导航](#23-公开页面导航)
- [第三章 核心操作手册（典型工作流）](#第三章-核心操作手册典型工作流)
  - [3.1 工作流一：从告警到处置的完整流程](#31-工作流一从告警到处置的完整流程)
  - [3.2 工作流二：一次完整的攻击调查流程](#32-工作流二一次完整的攻击调查流程)
  - [3.3 工作流三：AI驱动的自动化运营闭环](#33-工作流三ai驱动的自动化运营闭环)
- [第四章 各模块详细使用说明](#第四章-各模块详细使用说明)
  - [4.1 AI 分析工作台（/ai-analysis）](#41-ai-分析工作台aianalysis)
  - [4.2 安全大屏（/screen）](#42-安全大屏screen)
  - [4.3 运营仪表盘（/dashboard）](#43-运营仪表盘dashboard)
  - [4.4 运营指标（/metrics）](#44-运营指标metrics)
  - [4.5 通知中心（/notifications）](#45-通知中心notifications)
  - [4.6 信号流（/signals）](#46-信号流signals)
  - [4.7 调查/案件（/investigate）](#47-调查案件investigate)
  - [4.8 处置响应（/response）](#48-处置响应response)
  - [4.9 反馈学习（/learning）](#49-反馈学习learning)
  - [4.10 知识库（/knowledge）](#410-知识库knowledge)
  - [4.11 威胁狩猎（/hunting）](#411-威胁狩猎hunting)
  - [4.12 自动化编排（/workflows）](#412-自动化编排workflows)
  - [4.13 数据源管理（/datasource）](#413-数据源管理datasource)
  - [4.14 资产管理（/assets）](#414-资产管理assets)
  - [4.15 用户管理（/users）](#415-用户管理users)
  - [4.16 报告中心（/reports）](#416-报告中心reports)
  - [4.17 审计日志（/audit）](#417-审计日志audit)
  - [4.18 集成中心（/integrations）](#418-集成中心integrations)
  - [4.19 系统设置（/system）](#419-系统设置system)
  - [4.20 AI 调查助手（/ai-chat）](#420-ai-调查助手ai-chat)
- [第五章 AI 功能使用指南](#第五章-ai-功能使用指南)
  - [5.1 AI 研判引擎原理](#51-ai-研判引擎原理)
  - [5.2 AI 调查助手对话式操作](#52-ai-调查助手对话式操作)
  - [5.3 反馈学习系统](#53-反馈学习系统)
  - [5.4 AI 自动处置策略](#54-ai-自动处置策略)
- [第六章 集成与配置](#第六章-集成与配置)
  - [6.1 数据源接入配置](#61-数据源接入配置)
  - [6.2 Webhook 配置](#62-webhook-配置)
  - [6.3 SOAR 剧本编排](#63-soar-剧本编排)
- [第七章 管理与运维](#第七章-管理与运维)
  - [7.1 用户与权限管理](#71-用户与权限管理)
  - [7.2 审计日志查看](#72-审计日志查看)
  - [7.3 系统设置与维护](#73-系统设置与维护)
- [第八章 常见问题 FAQ](#第八章-常见问题-faq)
- [附录 A：术语表](#附录-a术语表)
- [附录 B：快捷键参考](#附录-b快捷键参考)
- [附录 C：错误码速查表](#附录-c错误码速查表)

---

## 第一章 产品简介与快速开始

### 1.1 产品概述

SecMind 是一款 **AI 驱动的智能安全运营平台**，面向企业安全运营中心（SOC）团队，提供从威胁检测、AI 智能研判、事件调查、自动处置到反馈学习的全链路安全运营能力。

平台核心价值主张：

| 维度 | 能力描述 |
|------|----------|
| **AI 原生研判** | 基于 5 阶段推理链（事件发现→证据关联→模式匹配→置信度评估→处置决策），自动完成攻击意图判定 |
| **全生命周期覆盖** | 从原始信号采集到最终处置闭环，覆盖安全事件的完整生命周期 |
| **人机协同** | AI 自动处置 + 人工审批复核，兼顾效率与安全 |
| **ATT&CK 对齐** | 威胁狩猎模块基于 MITRE ATT&CK 框架，支持战术/技术级假设验证 |
| **持续进化** | 通过反馈学习机制，AI 研判准确率随运营数据积累持续提升 |

平台共包含 **28 个功能页面**，分为公开页面（4 个）、认证页面（4 个）、后台功能页面（20 个）三大类。

### 1.2 技术架构

```
┌─────────────────────────────────────────────┐
│              前端 (Next.js 15)                │
│   React 19 + TypeScript + Tailwind CSS       │
│   ECharts 可视化 / shadcn/ui 组件库           │
├─────────────────────────────────────────────┤
│              后端 (Python FastAPI)            │
│   SQLAlchemy ORM / SQLite + PostgreSQL       │
│   JWT 认证 / bcrypt 密码哈希                  │
├─────────────────────────────────────────────┤
│              数据层                           │
│   devices(数据源) / users(用户)               │
│   alerts(告警) / cases(案件) / audit_logs     │
│   i18n 多语言 (zh/en)                         │
└─────────────────────────────────────────────┘
```

**技术栈详情：**

- **前端框架**：Next.js 15 (App Router) + React 19
- **UI 组件库**：shadcn/ui + Lucide Icons
- **状态管理**：Zustand (locale-store, user-store)
- **图表引擎**：ECharts (echarts-for-react)
- **后端框架**：Python FastAPI
- **数据库**：SQLite（开发）/ PostgreSQL（生产）
- **认证方式**：JWT Token + SSO 回调支持
- **国际化**：内置中文/英文双语支持

### 1.3 安装部署

#### 1.3.1 环境要求

| 组件 | 最低要求 | 推荐配置 |
|------|----------|----------|
| 操作系统 | Linux/macOS/Windows | Ubuntu 22.04 LTS |
| Node.js | ≥ 18.x | ≥ 20.x LTS |
| Python | ≥ 3.10 | ≥ 3.12 |
| 内存 | 4 GB | 8 GB+ |
| 磁盘空间 | 10 GB | 50 GB+ SSD |
| 浏览器 | Chrome 90+ / Firefox 88+ / Edge 90+ | 最新版 |

#### 1.3.2 快速部署步骤

```bash
# 1. 克隆项目
git clone <repository-url>
cd SecMind

# 2. 启动后端服务
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 3. 启动前端开发服务（新终端）
cd frontend
npm install
npm run dev

# 4. 访问平台
# 前端: http://localhost:3000
# 后端 API: http://localhost:8000
# API 文档: http://localhost:8000/docs
```

#### 1.3.3 生产环境部署

```bash
# 前端构建
cd frontend
npm run build
npm start  # 生产模式启动于端口 3000

# 后端生产启动（建议使用 Gunicorn + Uvicorn）
cd backend
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

> **注意**：生产环境请务必修改 `backend/.env` 中的以下配置项：
> - `SECRET_KEY`：JWT 签名密钥（必须为高强度随机字符串）
> - `DATABASE_URL`：切换至 PostgreSQL 连接串
> - `CORS_ORIGINS`：限制为实际前端域名

### 1.4 首次登录与引导流程

#### 1.4.1 注册账号

1. 访问平台首页 `http://your-domain`，点击右上角 **「注册」** 按钮
2. 进入注册页面 `/register`，填写以下信息：

| 字段 | 说明 | 要求 |
|------|------|------|
| 用户名 | 登录标识 | 3-20 字符，字母数字下划线 |
| 邮箱 | 用于通知和找回密码 | 合法邮箱格式 |
| 密码 | 登录凭证 | ≥8 位，含大小写字母和数字 |
| 确认密码 | 二次确认 | 与密码一致 |

3. 点击 **「注册」** 按钮，系统自动创建账号并跳转至登录页

#### 1.4.2 登录系统

1. 访问 `/login` 页面
2. 输入用户名（或邮箱）和密码
3. 点击 **「登录」** 或按 `Enter` 键
4. 支持 SSO 单点登录：通过 `/auth/callback` 回调完成第三方认证

#### 1.4.3 新手引导流程（Onboarding Guide）

首次成功登录后，系统会自动弹出 **5 步引导向导**，帮助新用户快速熟悉平台核心功能：

| 步骤 | 标题 | 内容要点 |
|------|------|----------|
| **Step 1** | 欢迎 | 平台介绍："SecMind 将 AI 能力注入安全运营的每个环节" |
| **Step 2** | AI 分析工作台 | 核心入口介绍：AI 研判界面是平台的核心，所有告警在此完成智能分析 |
| **Step 3** | 信号 → 告警 → 处置 | 生命周期说明：原始信号经 AI 筛选升级为告警，再触发自动或人工处置 |
| **Step 4** | 反馈驱动进化 | 学习机制说明：每次人工反馈都会让 AI 判断更精准 |
| **Step 5** | 开始使用 | 引导结束提示：可随时通过右上角菜单重新打开引导 |

**操作要点：**
- 每步引导卡片位于屏幕中央，带有半透明背景遮罩高亮目标区域
- 底部提供 **「上一步」** / **「下一步」** / **「跳过」** 三个操作按钮
- 最后一步提供 **「开始使用」** 主按钮，点击后关闭引导并进入仪表盘
- 完成引导后，引导状态会被持久化存储（localStorage），后续登录不再自动弹出
- 如需重新查看引导，点击右上角头像旁的 **「帮助」** 图标即可

---

## 第二章 界面导航说明

### 2.1 整体布局

后台页面采用经典的 **左右分栏布局**：

```
┌──────────┬──────────────────────────────────────┐
│          │  顶部导航栏 (Header)                   │
│          │  [Logo] [面包屑] [搜索] [通知] [头像]   │
│  侧边栏   ├──────────────────────────────────────┤
│ Sidebar  │                                      │
│          │         主内容区 (Main Content)        │
│ [5大分组] │                                      │
│ [20个菜单]│                                      │
│          │                                      │
│          │                                      │
└──────────┴──────────────────────────────────────┘
```

**布局区域说明：**

| 区域 | 位置 | 功能 |
|------|------|------|
| **顶部导航栏** | 页面最上方固定 | 包含 Logo、面包屑导航、全局搜索、通知铃铛、用户头像/语言切换 |
| **侧边栏** | 左侧固定宽度（可折叠） | 5 大功能分组、20 个功能菜单入口 |
| **主内容区** | 右侧自适应区域 | 当前选中功能的页面内容 |

**顶部导航栏详细元素（从左到右）：**

1. **SecMind Logo** — 点击返回仪表盘首页
2. **面包屑导航** — 显示当前页面路径（如：运营总览 > AI分析工作台）
3. **全局搜索框** — 支持模糊搜索告警、案件、IOC 等
4. **通知铃铛** — 显示未读通知数量红点，点击展开通知下拉面板
5. **语言切换** — 中文 / English 切换按钮
6. **用户头像** — 下拉菜单包含：个人资料、偏好设置、退出登录

### 2.2 侧边栏五大功能分组

侧边栏按业务领域划分为 **5 个功能分组**，每个分组用分隔线和分组标题区分。以下是完整的菜单结构：

---

#### 分组一：运营总览（Operations Overview）

此分组提供安全运营的全局视角和数据概览。

| 序号 | 菜单名称 | 路由路径 | 图标 | 功能简述 |
|------|----------|----------|------|----------|
| 1 | AI 分析工作台 | `/ai-analysis` | 🧠 Brain | 核心 AI 研判界面，告警汇聚与智能分析 |
| 2 | 安全大屏 | `/screen` | 📺 Monitor | 态势感知大屏展示，实时安全态势 |
| 3 | 运营仪表盘 | `/dashboard` | 📊 BarChart3 | 运营 KPI 数据概览与趋势分析 |
| 4 | 运营指标 | `/metrics` | 🎯 Target | MTTD/MTTR 等核心运营指标追踪 |
| 5 | 通知中心 | `/notifications` | 🔔 Bell | 系统通知、告警通知、任务提醒聚合 |

**使用场景**：安全分析师每日上班首先查看此分组，快速了解当前安全态势和待处理事项。

---

#### 分组二：事件生命周期（Incident Lifecycle）

此分组覆盖安全事件从信号采集到处置响应的完整生命周期。

| 序号 | 菜单名称 | 路由路径 | 图标 | 功能简述 |
|------|----------|----------|------|----------|
| 1 | 信号流 | `/signals` | 📡 Radio | 原始安全信号列表，多源数据统一视图 |
| 2 | 调查/案件 | `/investigate` | 🔍 Search | 案件调查工作台，证据关联与分析 |
| 3 | 处置响应 | `/response` | ⚡ Zap | 自动处置执行与策略管理 |

**使用场景**：这是日常安全运营的核心工作区，大部分调查和处置操作在此完成。

---

#### 分组三：AI 能力（AI Capabilities）

此分组展示平台的 AI 核心能力和持续进化机制。

| 序号 | 菜单名称 | 路由路径 | 图标 | 功能简述 |
|------|----------|----------|------|----------|
| 1 | 反馈学习 | `/learning` | 📈 TrendingUp | AI 反馈学习系统，人机协同优化 |
| 2 | 知识库 | `/knowledge` | 📚 BookOpen | 安全知识库，规则与经验沉淀 |

**使用场景**：安全运营负责人定期查看 AI 的学习效果和知识积累情况。

---

#### 分组四：增值模块（Value-Added Modules）

高级安全能力模块，面向有深度安全需求的企业。

| 序号 | 菜单名称 | 路由路径 | 图标 | 功能简述 |
|------|----------|----------|------|----------|
| 1 | 威胁狩猎 | `/hunting` | 🎯 Crosshair | ATT&CK 框架驱动的主动威胁狩猎 |
| 2 | 自动化编排 | `/workflows` | ⚙️ Workflow | SOAR 剧本引擎，自动化 playbook 编排 |

**使用场景**：高级威胁猎人和安全编排工程师使用。

---

#### 分组五：平台管理（Platform Management）

系统管理员使用的管理和配置功能。

| 序号 | 菜单名称 | 路由路径 | 图标 | 功能简述 |
|------|----------|----------|------|----------|
| 1 | 数据源 | `/datasource` | 🖥️ Server | 数据源接入与管理 |
| 2 | 资产管理 | `/assets` | 🛡️ Shield | 安全资产管理 |
| 3 | 用户管理 | `/users` | 👥 Users | 用户与权限管理 |
| 4 | 报告中心 | `/reports` | 📄 FileText | 报告生成与导出 |
| 5 | 审计日志 | `/audit` | 📋 ClipboardList | 操作审计追踪 |
| 6 | 集成中心 | `/integrations` | 🔗 Link | 第三方系统集成 |
| 7 | 系统设置 | `/system` | ⚙️ Settings | 系统参数配置 |

**独立入口（不在侧边栏分组内）：**
| 菜单名称 | 路径 | 说明 |
|----------|------|------|
| **AI 调查助手** | `/ai-chat` | 对话式 AI 协作调查（⭐ 新增功能） |

---

### 2.3 公开页面导航

无需登录即可访问的公开页面：

| 页面 | 路径 | 功能 |
|------|------|------|
| **首页/落地页** | `/` | 产品介绍、核心价值主张、CTA（注册/登录）按钮 |
| **解决方案** | `/solutions` | 行业解决方案展示（金融、政府、医疗等） |
| **定价** | `/pricing` | 三档定价方案（基础版/专业版/企业版）对比 |
| **文档中心** | `/docs` | API 文档、部署指南、FAQ |
| **使用指南** | `/docs/guide` | 本文档所在位置 |

---

## 第三章 核心操作手册（典型工作流）

本章通过三个典型工作流，串联平台各模块的使用方法，帮助用户建立完整的操作心智模型。

### 3.1 工作流一：从告警到处置的完整流程

**场景**：EDR 检测到一台终端出现异常行为，需要完成从信号确认到处置闭环的全过程。

#### 步骤 1：在信号流中发现异常信号

**入口**：侧边栏 → 事件生命周期 → **信号流** (`/signals`)

**操作过程**：
1. 打开信号流页面，查看原始信号列表
2. 使用筛选器过滤：选择来源 = `EDR`，级别 = `High`
3. 在列表中定位到目标信号（如：`SIG-20260509-0015`）
4. 查看信号详情：时间戳、来源设备、原始日志内容、协议类型
5. 点击 **「升级为告警」** 按钮，将信号升级为正式告警

**输入**：信号 ID、筛选条件
**输出**：升级后的告警编号（如 `ALT-20260509-0042`）

**注意事项**：
- 信号流中的每条记录代表一个原始安全事件，来自防火墙、EDR、IDS 等不同数据源
- 不是所有信号都需要升级为告警，AI 会自动筛选高价值信号
- 升级操作不可撤销，但可以在告警阶段标记为误报

#### 步骤 2：在 AI 分析工作台进行研判

**入口**：侧边栏 → 运营总览 → **AI 分析工作台** (`/ai-analysis`) 或直接点击告警卡片跳转

**操作过程**：
1. 进入 AI 分析工作台，看到升级后的告警已出现在告警列表中
2. 告警卡片显示关键信息：标题、严重级别、来源、AI 置信度、时间戳
3. 点击告警卡片展开 **AI 研判面板**，查看完整的 5 阶段推理链：
   - **事件发现**：检测到了什么异常（如 PowerShell 编码执行）
   - **证据关联**：多源证据交叉验证（VPN 日志 + EDR + 防火墙流量）
   - **模式匹配**：MITRE ATT&CK 技术映射（T1059.001 → T1071.001）
   - **置信度评估**：贝叶斯推理计算最终置信度（如 92%）
   - **处置决策**：推荐处置动作（冻结账号、隔离终端等）
4. 查看 **假设面板**：AI 生成的多个攻击假设及其置信度
5. 查看 **证据图谱**：实体关系图展示 IOC、资产、用户之间的关联
6. 确认研判结果后，点击 **「确认研判」** 或 **「提交处置」**

**输入**：告警 ID
**输出**：AI 研判结论（攻击类型、置信度、推荐处置动作）、假设列表、证据链

**注意事项**：
- AI 研判结论仅供参考，重要决策需人工复核
- 置信度 ≥ 85% 的告警会自动触发处置建议
- 可以在研判面板中添加人工备注和补充证据

#### 步骤 3：在处置响应页面执行处置

**入口**：侧边栏 → 事件生命周期 → **处置响应** (`/response`)

**操作过程**：
1. 进入处置响应页面，默认显示 **「自动处置」** 标签页
2. 在 **动作队列** 中找到待执行的处置动作：
   - 动作名称（如「冻结账号」「隔离主机」「封禁 IP」）
   - 目标对象（具体账号/IP/主机名）
   - 优先级（critical/high/medium/low）
   - 当前状态（pending/executing/completed/awaiting_approval）
   - AI 推理理由摘要
3. 展开动作卡片，查看 **完整推理链**（5 阶段详细推理过程）
4. 查看 **安全护栏信息**（审批要求、影响范围、回滚方案、审计记录）
5. 根据 **假设-动作映射** 面板了解该动作属于哪个攻击假设
6. 执行操作：
   - **pending 状态**：点击 **「执行」** 按钮立即执行
   - **awaiting_approval 状态**：点击 **「批准」** 或 **「拒绝」** 进行审批
   - **executing 状态**：可点击 **「取消」** 终止正在执行的动作
7. 执行后可在 **「执行记录」** 标签页查看历史处置日志
8. 如需回滚，在 **回滚记录** 区域执行回滚操作

**输入**：动作 ID、审批决定
**输出**：执行结果（成功/失败/超时）、执行日志、回滚选项

**注意事项**：
- critical 和 high 优先级的动作会在队列中置顶显示，带红色/橙色光晕效果
- 所有处置动作均记录完整审计日志，支持事后追溯
- 回滚操作需要填写原因并由授权人员执行

#### 流程图示

```
信号流(/signals) ──升级──→ AI分析工作台(/ai-analysis) ──研判──→ 处置响应(/response)
     │                        │                              │
     ▼                        ▼                              ▼
 原始信号列表             5阶段AI推理链                    动作队列执行
 筛选/搜索/升级          假设/证据/置信度                 审批/回滚/审计
```

---

### 3.2 工作流二：一次完整的攻击调查流程

**场景**：收到一条 VPN 异地登录的高危告警，需要进行完整的溯源调查。

#### 步骤 1：使用 AI 调查助手快速入门

**入口**：侧边栏独立入口或直接访问 `/ai-chat`

**操作过程**：
1. 打开 AI 调查助手页面，界面分为三个区域：
   - **左侧边栏**：历史对话列表 + 数据范围信息
   - **中间对话区**：聊天消息展示（支持滚动）
   - **底部输入框**：自然语言输入
2. 在快捷提问区域点击 **「分析最新告警」** 或直接输入：
   ```
   帮我分析一下最新的高危告警，给出攻击研判结论
   ```
3. AI 调查助手自动执行以下操作序列：
   - **工具调用**：调用 `query_auth_logs` 查询认证日志（显示工具调用卡片，含输入参数和返回结果）
   - **证据列表**：返回结构化证据，每条标注来源、时间戳、方向（支持/反对/中性）
   - **置信度更新**：展示调整前后的置信度变化条
   - **后续建议**：以建议芯片（Suggestion Chips）形式给出下一步行动选项
4. 基于返回的证据和建议，选择继续深入调查：
   - 点击建议芯片 **「联系员工确认是否本人操作」**
   - 或输入新的查询：`103.45.67.89 这个 IP 有什么威胁情报？`

**输入**：自然语言问题
**输出**：结构化回答 + 证据列表 + 工具调用记录 + 置信度更新 + 后续建议

**注意事项**：
- AI 调查助手支持 6 类快捷提问：告警分析、账号行为、IP 威胁查询、文件追溯、横向移动检测、态势总结
- 按 Enter 发送消息，Shift+Enter 换行
- 每条 AI 回复支持复制、点赞/点踩操作
- 对话历史保存在左侧边栏，支持新建对话和删除旧对话

#### 步骤 2：创建调查案件

**入口**：侧边栏 → 事件生命周期 → **调查/案件** (`/investigate`)

**操作过程**：
1. 在调查工作台中点击 **「新建案件」** 按钮
2. 填写案件基本信息：
   - 案件标题（如：「VPN 异地登录 - zhangsan 账号疑似失陷」）
   - 关联告警（从列表中选择或自动关联）
   - 优先级（P1-P4）
   - 负责人（分配给指定分析师）
   - 描述（初步调查发现）
3. 案件创建后进入 **案件详情页**，包含以下工作区：
   - **时间线视图**：按时间顺序排列的事件序列
   - **证据板**：拖拽式证据整理区域
   - **关联图谱**：实体关系可视化
   - **调查笔记**：自由文本记录区
4. 将 AI 调查助手提供的证据批量导入案件证据板

**输入**：案件信息表单
**输出**：案件编号（如 `CASE-2026-0042`）、案件工作台

#### 步骤 3：使用威胁狩猎深化分析

**入口**：侧边栏 → 增值模块 → **威胁狩猎** (`/hunting`)

**操作过程**：
1. 进入威胁狩猎页面，基于 MITRE ATT&CK 框架创建狩猎假设
2. 点击 **「新建假设」** 按钮，填写：
   - **假设名称**（如：「APT29 钓鱼攻击凭证窃取」）
   - **战术选择**（从 14 个 ATT&CK 战术中选择，如「初始访问」）
   - **技术选择**（对应战术下的具体技术，如「鱼叉式钓鱼 T1566.001」）
   - **初始置信度**和 **描述**
3. 假设创建后进入验证循环：
   - **IOC 收集**：添加相关 IOC（IP、域名、Hash、URL），系统自动查询威胁情报
   - **证据关联**：将信号流和调查案件中的证据关联到假设
   - **置信度更新**：根据新证据调整假设置信度
   - **状态流转**：验证中 → 已确认 / 已排除
4. 使用 ATT&CK 战术/技术矩阵视图，直观查看所有假设在框架中的分布

**输入**：狩猎假设定义、IOC 列表
**输出**：假设验证结果（已确认/已排除）、关联 IOC、威胁情报报告

**注意事项**：
- 威胁狩猎是主动式的威胁发现方法，区别于被动告警响应
- 每个假设必须映射到 ATT&CK 战术和技术 ID
- IOC 类型支持：IP、域名、Hash（MD5/SHA256）、URL
- 威胁情报查询整合了 VirusTotal、AlienVault OTX 等外部情报源

#### 步骤 4：完成调查并归档

1. 在调查案件中汇总所有发现
2. 生成调查报告（导出为 PDF）
3. 关联反馈学习条目（如果 AI 研判有偏差）
4. 将案件状态变更为 **「已关闭」**

---

### 3.3 工作流三：AI 驱动的自动化运营闭环

**场景**：配置 AI 自动处置策略，实现从告警到处置的全自动化运行。

#### 步骤 1：配置自动处置策略

**入口**：处置响应 (`/response`) → **「处置策略」** 标签页

**操作过程**：
1. 查看预置的 4 条自动处置策略：

| 策略名称 | 触发条件 | 处置链 | 置信度阈值 |
|----------|----------|--------|------------|
| 账号失陷自动处置 | 置信度 ≥ 85% | 冻结账号 → 重置密码 → 隔离终端 | 85% |
| C2 通信自动阻断 | 置信度 ≥ 90% | 封禁 IP → 隔离终端 → 通知团队 | 90% |
| 暴力破解自动防御 | 失败次数 ≥ 50 | 封禁 IP → 锁定账号 → 启用 MFA | 95% |
| 数据外泄自动遏制 | 外传 ≥ 100MB | 阻断连接 → 取证 → 通知 DLP | 80% |

2. 点击策略卡片查看详细配置：
   - **触发条件**：满足何种条件时激活
   - **处置动作链**：按序执行的多个动作
   - **置信度阈值**：AI 置信度达到多少时自动执行
   - **人工审批节点**：哪些环节需要人工介入
3. 点击 **「编辑策略」** 自定义或创建新策略

**输入**：策略配置（触发条件、动作链、阈值、审批节点）
**输出**：生效的自动处置策略

#### 步骤 2：配置 SOAR 剧本

**入口**：侧边栏 → 增值模块 → **自动化编排** (`/workflows`)

**操作过程**：
1. 进入 SOAR 剧本引擎页面，查看已有 Playbook 列表
2. 每个 Playbook 显示：
   - 名称和描述
   - 触发条件
   - 步骤数量
   - 总执行次数
   - 最近执行时间
   - 启用/禁用状态
3. 点击 Playbook 查看 **可视化流程图**：
   - **触发节点**（Trigger）：什么条件启动剧本
   - **条件节点**（Condition）：分支判断逻辑
   - **动作节点**（Action）：具体执行的操作
   - **审批节点**（Approval）：需人工确认的环节
   - 节点间连线表示执行流向
4. 点击 **「新建剧本」** 创建自定义 Playbook：
   - 选择触发器类型（定时器/告警/Webhook/手动）
   - 拖拽添加节点构建流程
   - 配置每个节点的参数
   - 设置错误处理和重试策略
5. 在 **执行记录** 标签页查看历史运行情况

**输入**：Playbook 定义（节点、连线、参数）
**输出**：可执行的自动化剧本

#### 步骤 3：建立反馈学习闭环

**入口**：侧边栏 → AI 能力 → **反馈学习** (`/learning`)

**操作过程**：
1. 进入反馈学习系统，查看 4 个标签页的数据：

**标签页 1 — 知识反馈概览**：
- 今日反馈数、确认率、平均置信度提升等统计卡片
- 近期反馈列表（案件 ID、攻击类型、人工操作、置信度变化）

**标签页 2 — 反馈学习明细**：
- 每条反馈的详细信息：原始 AI 评估 vs 人工修正
- 学习结果：AI 如何根据反馈调整判断
- 类型区分：确认类（confirmed）vs 争议类（disputed）

**标签页 3 — 推理校正**：
- 权重调整记录：哪些推理规则的权重被修改
- 模式改进：识别到的新攻击模式
- 校正前后置信度对比

**标签页 4 — AI 成长曲线**：
- 准确率随时间的变化趋势图
- 关键里程碑事件（首次超过 95%、处理第 1000 条等）
- 各攻击类型的识别准确率分布

2. 对于每条 AI 研判结果，操作人员可以：
   - **确认**：同意 AI 的判断（提高此类模式的权重）
   - **争议**：不同意 AI 的判断（降低权重并提供正确答案）
   - **补充**：添加额外上下文信息

3. 反馈提交后，AI 模型在下一次训练迭代中自动吸收这些反馈

**输入**：人工反馈（确认/争议 + 备注）
**输出**：AI 模型参数微调、准确率提升

**闭环示意**：

```
告警产生 → AI研判 → 自动/人工处置 → 人工反馈 → 模型优化 → 更精准的研判 ↑
    ↑                                                                    │
    └────────────────── 循环往复，持续进化 ──────────────────────────────┘
```

---

## 第四章 各模块详细使用说明

### 4.1 AI 分析工作台（/ai-analysis）

**功能定位**：平台的核心 AI 研判界面，所有安全告警在此汇聚并进行智能分析。

**入口位置**：侧边栏 → 运营总览 → AI 分析工作台

#### 4.1.1 页面布局

页面采用 **三栏式布局**：

```
┌─────────────┬──────────────────┬─────────────────┐
│             │                  │                 │
│  告警列表    │   AI 研判面板     │   假设面板      │
│  (左侧)     │   (中间主区域)    │   (右侧)        │
│             │                  │                 │
│ • 告警卡片   │ • 5阶段推理链     │ • 攻击假设列表   │
│ • 筛选器    │ • 证据详情        │ • 置信度指示     │
│ • 搜索      │ • MITRE 映射      │ • 关联动作       │
│             │ • 置信度评分      │                 │
└─────────────┴──────────────────┴─────────────────┘
```

#### 4.1.2 告警列表（左侧栏）

**功能**：展示所有待分析和已分析的告警卡片。

**字段说明**：

| 字段 | 描述 | 示例 |
|------|------|------|
| 告警 ID | 唯一标识符 | ALT-20260509-0042 |
| 标题 | 告警简要描述 | 主机 WIN-02 检测到 PowerShell 编码执行 |
| 严重级别 | Critical / High / Medium / Low / Info | 用颜色标签区分 |
| 来源 | 数据源类型 | EDR / 防火墙 / VPN网关 |
| AI 置信度 | AI 判定为真实攻击的概率 | 92%（进度条可视化） |
| 时间戳 | 告警产生时间 | 2026-05-09 08:18:05 |
| 状态 | analyzing / confirmed / false_positive / escalating | 分析中/已确认/误报/升级中 |

**交互操作**：
- **单击**告警卡片 → 在中间区域加载对应的 AI 研报面板
- **筛选器**：按严重级别、来源、时间范围、状态筛选
- **搜索框**：模糊搜索告警标题和 ID
- **排序**：按时间、置信度、严重级别排序

#### 4.1.3 AI 研判面板（中间主区域）

**功能**：展示 AI 对选中告警的完整推理分析过程。

**五阶段推理链详解**：

| 阶段 | 图标 | 名称 | 说明 |
|------|------|------|------|
| Phase 1 | 🔍 Search | 事件发现 | 描述检测到的异常事件是什么 |
| Phase 2 | 🔗 Link2 | 证据关联 | 将来自多个数据源的证据进行交叉关联 |
| Phase 3 | 🎯 Target | 模式匹配 | 与已知攻击模式和 MITRE ATT&CK 技术进行匹配 |
| Phase 4 | 📊 BarChart3 | 置信度评估 | 基于贝叶斯推理计算综合置信度 |
| Phase 5 | 💡 Lightbulb | 处置决策 | 基于置信度和策略推荐处置动作 |

**每个推理阶段的展示内容包括**：
- 阶段标题和编号
- 自然语言描述
- 证据列表（每条证据含：来源、时间戳、详情、方向）
- 该阶段的结论
- （可选）MITRE ATT&CK 技术引用编号
- （可选）置信度增量

**证据方向说明**：

| 方向 | 含义 | 颜色 |
|------|------|------|
| supports | 支持攻击假设 | 绿色 |
| contradicts | 反对攻击假设 | 红色 |
| neutral | 中性，既不支持也不反对 | 灰色 |

#### 4.1.4 假设面板（右侧栏）

**功能**：展示 AI 为当前告警生成的多个攻击假设。

**字段说明**：

| 字段 | 描述 |
|------|------|
| 假设 ID | H-A、H-B、H-C 等标识 |
| 假设标签 | 攻击类型描述（如「账号被盗」、「内部人员威胁」、「误报」） |
| 置信度 | 该假设成立的概率（百分比） |
| 关联动作 | 属于此假设的处置动作列表及状态 |
| 进度条 | 显示已完成/总数/执行中/待处理/等待审批的数量 |

**视觉指示**：
- 置信度 ≥ 80%：绿色高亮，带发光效果
- 置信度 50-80%：黄色/琥珀色
- 置信度 < 50%：灰色

#### 4.1.5 证据图谱

**功能**：以图形化方式展示证据之间的实体关系。

**节点类型**：
- **IOC 节点**（IP、域名、Hash）：菱形，红色边框
- **资产节点**（主机名、服务器）：方形，蓝色边框
- **用户节点**（账号）：圆形，绿色边框
- **事件节点**（告警、信号）：六边形，橙色边框

**连线含义**：表示两个实体之间存在关联关系（如「登录自」「访问过」「通信」等）。

#### 4.1.6 注意事项

- AI 研判结论的置信度是一个概率值，不是确定性判断
- 当存在矛盾证据时（同时有支持和反对的证据），AI 会降低置信度并建议人工介入
- 所有推理过程完全透明可追溯，每个结论都有对应的证据支撑
- 研判面板支持导出为 PDF 报告

---

### 4.2 安全大屏（/screen）

**功能定位**：安全态势感知大屏，用于安全运营中心（SOC）的大屏展示场景。

**入口位置**：侧边栏 → 运营总览 → 安全大屏

#### 4.2.1 页面特点

- **全屏沉浸式设计**：无侧边栏，最大化利用屏幕空间
- **深色主题**：适合长时间观看和投影展示
- **实时数据**：数据自动刷新（默认 30 秒间隔）
- **地理可视化**：全球攻击来源地图

#### 4.2.2 大屏区域划分

| 区域 | 内容 | 更新频率 |
|------|------|----------|
| **顶部标题栏** | 平台名称、当前时间、在线状态 | 实时 |
| **左侧面板** | 告警统计、严重级别分布、Top 5 攻击类型 | 30s |
| **中央地图区** | 全球攻击来源热力图、实时攻击流 | 10s |
| **右侧面板** | 活跃威胁列表、处置状态、KPI 指标 | 30s |
| **底部面板** | 趋势图表（24h/7d/30d）、资源利用率 | 60s |

#### 4.2.3 操作说明

- **全屏切换**：按 `F11` 或点击全屏按钮
- **刷新数据**：手动点击刷新按钮
- **导出截图**：保存当前大屏状态为图片
- **时间范围切换**：24 小时 / 7 天 / 30 天

---

### 4.3 运营仪表盘（/dashboard）

**功能定位**：安全运营的核心数据概览页面，展示 KPI 和关键运营指标。

**入口位置**：侧边栏 → 运营总览 → 运营仪表盘

#### 4.3.1 核心 KPI 卡片（顶部四宫格）

| KPI 指标 | 图标 | 说明 | 颜色编码 |
|----------|------|------|----------|
| **今日告警** | Bell | 当日产生的告警总量 | 红色（critical） |
| **待处理案件** | Briefcase | 等待分析师处理的案件数 | 橙色（high） |
| **活跃威胁** | ShieldAlert | 当前仍在活动的威胁数量 | 黄色（medium） |
| **AI 分析中** | Brain | 正在被 AI 处理的告警数 | 青色（primary） |

每个 KPI 卡片显示：
- 当前数值（大字号加粗）
- 较昨日变化趋势（↑ 上升 / ↓ 下降，带百分比）
- 一行副标题说明
- 快捷操作链接（如「查看详情」「进入工单」）

#### 4.3.2 最高优先级告警列表

位于 KPI 卡片下方，展示需要立即关注的告警：

| 字段 | 说明 |
|------|------|
| 时间 | 告警触发时间（精确到秒） |
| 级别 | Critical/High/Medium 带颜色标签 |
| 来源 | EDR/防火墙/WAF/IDS 等 |
| 标题 | 告警描述 |
| 描述 | 详细说明 |
| 状态 | analyzing/confirmed/false_positive |
| 负责人 | 分配的分析师 |

Critical 级别告警带有红色脉冲动画效果，提醒立即关注。

#### 4.3.3 其他 Dashboard 组件

- **告警趋势图**：折线图展示过去 7 天/30 天的告警量变化
- **来源分布饼图**：各数据源告警占比
- **处置效率图**：MTTD/MTTR 趋势
- **最近活动流**：最新的操作和事件时间线
- **快捷操作面板**：常用功能一键入口

---

### 4.4 运营指标（/metrics）

**功能定位**：专门的安全运营效能指标追踪页面。

**入口位置**：侧边栏 → 运营总览 → 运营指标

#### 4.4.1 核心指标

| 指标 | 全称 | 说明 | 目标值 |
|------|------|------|--------|
| **MTTD** | Mean Time To Detect | 平均检测时间（从攻击发生到被发现） | < 10 min |
| **MTTR** | Mean Time To Respond | 平均响应时间（从发现到开始处置） | < 30 min |
| **MTTA** | Mean Time To Analyze | 平均分析时间（AI 研判耗时） | < 2 min |
| **自动处置率** | Auto-Remediation Rate | 由 AI 自动完成的处置比例 | > 70% |
| **误报率** | False Positive Rate | 被判定为误报的告警比例 | < 5% |
| **AI 准确率** | AI Accuracy | AI 研判结论的正确比例 | > 95% |

#### 4.4.2 图表展示

- **指标趋势折线图**：每个指标的日/周/月趋势
- **同比/环比对比**：与上周/上月对比
- **SLA 达标率**：各指标是否达到设定的 SLA 目标
- **团队效能排行**：分析师个人的处置效率排名

---

### 4.5 通知中心（/notifications）

**功能定位**：系统通知、告警通知和任务提醒的统一聚合中心。

**入口位置**：侧边栏 → 运营总览 → 通知中心

#### 4.5.1 通知类型

| 类型 | 图标 | 触发条件 | 优先级 |
|------|------|----------|--------|
| 告警通知 | AlertTriangle | 新产生 Critical/High 级别告警 | 高 |
| 处置通知 | Zap | 自动处置执行完成或需要审批 | 高 |
| 系统通知 | Bell | 系统维护、版本更新等 | 中 |
| 任务提醒 | Clock | 待办任务到期提醒 | 中 |
| 反馈请求 | MessageSquare | 需要 AI 反馈学习的案件 | 低 |

#### 4.5.2 操作功能

- **全部已读**：一键标记所有通知为已读
- **按类型筛选**：只查看特定类型的通知
- **批量操作**：批量删除/归档通知
- **通知设置**：配置每种通知的接收方式（站内/邮件/ webhook）

---

### 4.6 信号流（/signals）

**功能定位**：原始安全信号的统一视图，是多源安全数据的汇聚入口。

**入口位置**：侧边栏 → 事件生命周期 → 信号流

#### 4.6.1 信号列表字段

| 字段 | 说明 | 示例 |
|------|------|------|
| 信号 ID | 唯一标识 | SIG-20260509-0015 |
| 时间戳 | 信号产生时间 | 2026-05-09 08:15:23 |
| 来源 | 数据源设备 | VPN网关 / EDR传感器 / 防火墙 |
| 类型 | 信号分类 | auth_failure / malware_detect / network_anomaly |
| 级别 | info / low / medium / high / critical | high |
| 摘要 | 信号内容摘要 | 异常地理位置 VPN 登录 |
| 原始日志 | 完整原始日志内容 | （可展开查看） |
| 状态 | new / analyzed / escalated / dismissed | new |

#### 4.6.2 操作按钮

| 按钮 | 功能 | 说明 |
|------|------|------|
| **查看详情** | 展开信号完整信息 | 含原始日志、解析字段、关联告警 |
| **升级为告警** | 将信号提升为正式告警 | 触发 AI 研判流程 |
| **忽略** | 标记为无关信号 | 不进入告警流程 |
| **导出** | 导出选中的信号 | CSV/JSON 格式 |

#### 4.6.3 筛选与搜索

- **时间范围选择器**：最近 1 小时 / 6 小时 / 24 小时 / 7 天 / 自定义
- **来源筛选**：按数据源设备类型筛选
- **级别筛选**：按严重级别筛选
- **全文搜索**：支持在原始日志内容中搜索关键词
- **高级搜索**：组合条件查询（AND/OR/NOT）

---

### 4.7 调查/案件（/investigate）

**功能定位**：安全事件调查的专用工作台，提供案件管理、证据组织和关联分析能力。

**入口位置**：侧边栏 → 事件生命周期 → 调查/案件

#### 4.7.1 案件列表

| 字段 | 说明 |
|------|------|
| 案件 ID | CASE-YYYYMMDD-NNNN 格式 |
| 标题 | 案件描述性标题 |
| 优先级 | P1（最高）~ P4（最低） |
| 状态 | open / in_progress / waiting / closed |
| 负责人 | 主责分析师 |
| 创建时间 | 案件创建时间 |
| 关联告警数 | 关联的告警数量 |
| 最后更新 | 最后操作时间 |

#### 4.7.2 案件详情页工作区

**时间线视图**：
- 按时间倒序排列所有相关事件
- 支持缩放（1h / 6h / 24h / 7d）
- 每个事件可展开查看详情
- 关键事件可添加标注

**证据板**：
- 类似 Kanban 板的证据组织方式
- 支持拖拽排序
- 证据分类：直接证据 / 间接证据 / 旁证
- 每条证据可附加标签和备注

**关联图谱**：
- 与 AI 分析工作台的证据图谱类似
- 但范围扩展到整个案件的所有实体
- 支持手动添加/删除节点和连线

**调查笔记**：
- Markdown 格式的自由文本记录
- 支持插入图片、附件
- 自动保存，支持版本历史

---

### 4.8 处置响应（/response）

**功能定位**：自动处置执行中心和策略管理界面。

**入口位置**：侧边栏 → 事件生命周期 → 处置响应

#### 4.8.1 三个标签页

**标签页一：自动处置（Auto Disposal）**

**顶部统计卡（四宫格）**：

| 指标 | 说明 |
|------|------|
| 待执行动作 | 状态为 pending 的动作数量 |
| 已执行 | 历史累计执行成功的动作数 |
| 待审批 | 状态为 awaiting_approval 的动作数量 |
| 失败 | 执行失败的动作数（带红色警告光晕） |

**动作队列（主列表）**：

每条动作卡片包含以下信息：

| 区域 | 内容 |
|------|------|
| **头部** | 动作图标 + 名称 + 优先级标签 + 状态标签 |
| **AI 推理** | AI 生成该处置动作的理由（青色渐变背景） |
| **推理链** | 可展开的 5 阶段详细推理过程（含证据、MITRE 引用、置信度） |
| **安全护栏** | 四宫格：审批要求、影响范围、回滚方案、审计记录 |
| **假设归属** | 显示属于哪个攻击假设及置信度 |
| **元信息** | 目标对象、请求来源、时间戳 |
| **操作按钮** | 执行 / 批准 / 拒绝 / 取消（根据状态动态显示） |

**动作状态说明**：

| 状态 | 含义 | 可执行操作 |
|------|------|------------|
| pending | 等待执行 | 点击「执行」 |
| executing | 正在执行中 | 点击「取消」终止 |
| completed | 执行完毕 | 只读 |
| failed | 执行失败 | 查看错误信息，重试 |
| awaiting_approval | 等待人工审批 | 点击「批准」或「拒绝」 |

**右侧面板 — 假设-动作映射**：
- 显示所有攻击假设及其置信度
- 每个假设下列出关联的处置动作
- 进度条显示完成比例
- 四格统计：已完成 / 执行中 / 待处理 / 等待审批

**标签页二：处置策略（Disposal Strategy）**

展示 AI 自动处置的策略配置：

- **执行逻辑说明**：置信度 → 优先级映射规则
- **预置策略列表**：4 条内置策略的详细配置
- 每条策略显示：名称、触发条件、处置动作链、置信度阈值进度条

**标签页三：执行记录（Execution Records）**

- **执行日志时间线**：按时间顺序排列的历史执行记录
- 每条记录显示：时间、动作、状态、假设归属、置信度、执行者（AI引擎/人工）
- **回滚记录**：独立的回滚操作历史，含原因和操作人

#### 4.8.2 处置动作类型

| 动作名称 | 目标类型 | 说明 | 风险等级 |
|----------|----------|------|----------|
| 冻结账号 | 用户账号 | 禁用账号登录权限 | 🔴 高 |
| 隔离主机 | 终端设备 | 网络隔离受控设备 | 🔴 高 |
| 封禁 IP | IP 地址 | 在防火墙层阻止流量 | 🟠 中高 |
| 重置 VPN 凭证 | VPN 账号 | 作废并重新下发 VPN 凭证 | 🟠 中高 |
| 监控用户活动 | 用户账号 | 增强日志采集和行为监控 | 🟡 中 |
| 通知安全团队 | 通知渠道 | 发送告警通知邮件/消息 | 🟢 低 |
| 保全取证数据 | 终端设备 | 远程采集内存镜像和日志 | 🟢 低 |
| 审查访问日志 | 服务器 | 深度审查安全日志 | 🟢 低 |

---

### 4.9 反馈学习（/learning）

**功能定位**：AI 模型的反馈学习和持续优化中心。

**入口位置**：侧边栏 → AI 能力 → 反馈学习

#### 4.9.1 四个标签页

**标签页 1：知识反馈概览**

顶部统计卡片：
- 今日反馈数、确认率、平均置信度提升、活跃学习案例

近期反馈列表：
- 案件 ID、攻击类型、人工操作（确认攻击/标记误报）、置信度变化（调整前→调整后）

**标签页 2：反馈学习明细**

每条反馈记录包含：
- 原始 AI 评估结论和置信度
- 人工修正结论
- AI 学习 outcome（如何调整）
- 反馈类型：confirmed（确认类）/ disputed（争议类）
- 备注信息

**标签页 3：推理校正**

- 权重调整记录：规则名称、调整前权重 → 调整后权重
- 模式改进：发现的新攻击模式
- 校正前后置信度对比

**标签页 4：AI 成长曲线**

- 准确率趋势折线图（ECharts 绘制）
- 里程碑事件时间线
- 各攻击类型的识别准确率雷达图
- 累计处理量统计

#### 4.9.2 反馈操作流程

```
AI 研判结果产生
       ↓
  分析师审核结论
       ↓
  ┌──────────────┬──────────────┐
  │   同意 AI    │   不同意 AI   │
  │  (确认反馈)  │  (争议反馈)   │
  └──────────────┴──────────────┘
       ↓              ↓
  提高该模式权重   降低权重 + 提供正确答案
       ↓              ↓
  └──────────┬───────┘
             ↓
      模型参数微调
             ↓
       下次研判更准确
```

---

### 4.10 知识库（/knowledge）

**功能定位**：安全知识的沉淀和管理中心，存储攻击模式、处置规则和最佳实践。

**入口位置**：侧边栏 → AI 能力 → 知识库

#### 4.10.1 知识分类

| 分类 | 说明 | 示例 |
|------|------|------|
| 攻击模式库 | 已知的攻击手法和 TTPs | APT29 钓鱼链路、Cobalt Strike C2 |
| 处置规则库 | 标准化的处置操作规程 | 账号失陷 SOP、勒索病毒响应流程 |
| IOC 知识库 | 内部积累的威胁指标 | 恶意 IP、域名、Hash 黑名单 |
| 最佳实践 | 团队总结的经验文档 | 调查清单、报告模板 |
| 合规基线 | 法规和标准要求 | 等保 2.0、GDPR 相关检查项 |

#### 4.10.2 操作功能

- **新建知识条目**：支持富文本编辑、标签管理
- **知识搜索**：全文检索 + 标签过滤
- **版本管理**：知识条目的修改历史
- **导入/导出**：批量导入 STIX 格式威胁情报
- **AI 辅助编写**：基于已有案例自动生成知识条目草稿

---

### 4.11 威胁狩猎（/hunting）

**功能定位**：基于 MITRE ATT&CK 框架的主动威胁发现模块。

**入口位置**：侧边栏 → 增值模块 → 威胁狩猎

#### 4.11.1 ATT&CK 战术矩阵

平台覆盖的全部 14 个 ATT&CK 战术：

| # | 战术 | 英文 | 说明 |
|---|------|------|------|
| 1 | 侦察 | Reconnaissance | 收集目标信息 |
| 2 | 资源开发 | Resource Development | 构建攻击资源 |
| 3 | 初始访问 | Initial Access | 获取初始 foothold |
| 4 | 执行 | Execution | 在目标上运行代码 |
| 5 | 持久化 | Persistence | 维持长期访问 |
| 6 | 权限提升 | Privilege Escalation | 提升到更高权限 |
| 7 | 防御规避 | Defense Evasion | 绕过安全控制 |
| 8 | 凭证访问 | Credential Access | 窃取凭据 |
| 9 | 发现 | Discovery | 了解内网环境 |
| 10 | 横向移动 | Lateral Movement | 在内网中移动 |
| 11 | 收集 | Collection | 收集目标数据 |
| 12 | C2 | Command and Control | 与攻击者通信 |
| 13 | 数据外泄 | Exfiltration | 窃取数据出境 |
| 14 | 影响 | Impact | 对系统造成破坏 |

#### 4.11.2 狩猎假设管理

**假设字段**：

| 字段 | 说明 | 示例 |
|------|------|------|
| 假设 ID | HT-NNN 格式 | HT-001 |
| 名称 | 假设描述 | APT29 钓鱼攻击凭证窃取 |
| 战术 | ATT&CK 战术 | 初始访问 |
| 技术 | ATT&CK 技术 | 鱼叉式钓鱼 T1566.001 |
| 创建时间 | 假设建立时间 | 2026-05-10 09:12 |
| 状态 | 验证中 / 已确认 / 已排除 | 验证中 |
| IOC 数量 | 关联的 IOC 数量 | 8 |
| 置信度 | 假设成立概率 | 87% |

**假设状态与视觉**：

| 状态 | 颜色 | 图标 | 说明 |
|------|------|------|------|
| 验证中 | 黄色 | Clock | 正在收集证据验证 |
| 已确认 | 红色 | AlertTriangle | 假设成立，需处置 |
| 已排除 | 绿色 | CheckCircle2 | 假设不成立，关闭 |

**IOC 管理**：

| 字段 | 说明 |
|------|------|
| IOC 值 | IP / 域名 / Hash / URL |
| 类型 | 自动识别 |
| 威胁评分 | 0-100，越高越危险 |
| 关联情报 | 来自外部情报源的上下文 |
| 首次/最后 seen | 时间范围 |

#### 4.11.3 操作流程

1. **创建假设**：基于情报或异常现象提出狩猎假设
2. **定义 IOC**：添加相关的威胁指标
3. **收集证据**：从信号流、日志中收集支持/反对证据
4. **更新置信度**：根据证据调整假设置信度
5. **得出结论**：确认或排除假设
6. **联动处置**：已确认的假设自动关联到处置响应

---

### 4.12 自动化编排（/workflows）

**功能定位**：SOAR（安全编排、自动化与响应）剧本引擎。

**入口位置**：侧边栏 → 增值模块 → 自动化编排

#### 4.12.1 Playbook 结构

每个 Playbook（剧本）包含：

| 组件 | 说明 |
|------|------|
| 基本信息 | 名称、描述、触发条件、状态（启用/禁用） |
| 流程节点 | Trigger → Condition → Action → Approval |
| 执行统计 | 总执行次数、最近执行时间、成功率 |
| 执行记录 | 每次运行的详细日志 |

**节点类型**：

| 类型 | 图标形状 | 说明 |
|------|----------|------|
| trigger | 圆角矩形 | 剧本触发器（告警/定时器/Webhook/手动） |
| condition | 菱形 | 条件判断（if/else 分支） |
| action | 矩形 | 具体执行动作 |
| approval | 八边形 | 人工审批节点 |

#### 4.12.2 内置 Playbook

| Playbook 名称 | 触发条件 | 步骤数 | 说明 |
|---------------|----------|--------|------|
| 账号失陷自动处置 | 置信度 ≥ 85% | 5 | 冻结→隔离→审批→通知→取证 |
| C2 通信自动阻断 | 置信度 ≥ 90% | 4 | 封禁→隔离→通知→记录 |
| 暴力破解防御 | 失败次数 ≥ 50 | 4 | 封禁IP→锁定→MFA→通知 |
| 数据外泄遏制 | 外传 ≥ 100MB | 4 | 阻断→取证→DLP通知→报告 |
| WebShell 清除 | WebShell 检测 | 3 | 隔离→清除→验证 |

#### 4.12.3 操作功能

- **新建 Playbook**：可视化拖拽编辑器
- **启用/禁用**：快速切换剧本状态
- **测试运行**：沙箱环境测试
- **克隆**：基于现有剧本快速创建新剧本
- **导出/导入**：YAML 格式的剧本交换

---

### 4.13 数据源管理（/datasource）

**功能定位**：安全数据源的接入、配置和健康监控中心。

**入口位置**：侧边栏 → 平台管理 → 数据源

#### 4.13.1 支持的数据源类型

| 类型 | 说明 | 协议 |
|------|------|------|
| 防火墙 | 网络边界防护设备 | Syslog / CEF |
| VPN 网关 | 远程访问网关 | Syslog / API |
| WAF | Web 应用防火墙 | Syslog / CEF |
| IDS/IPS | 入侵检测/防御系统 | Syslog / CEF |
| EDR | 端点检测与响应 | Agent / API |
| NAC | 网络准入控制 | Syslog |
| SIEM | 安全信息事件管理 | API / Syslog |
| 邮件网关 | 邮件安全网关 | Syslog / API |
| DLP | 数据防泄漏 | Syslog / API |

#### 4.13.2 添加数据源操作步骤

1. 点击 **「+ 添加数据源」** 按钮
2. 在弹出的对话框中填写：

| 字段 | 必填 | 说明 | 示例 |
|------|------|------|------|
| 设备名称 | ✅ | 自定义名称 | 「生产环境防火墙-01」 |
| 设备类型 | ✅ | 从下拉列表选择 | 防火墙 |
| 品牌 | ❌ | 设备厂商 | Fortinet |
| 型号 | ❌ | 设备型号 | FortiGate-600F |
| IP 地址 | ✅ | 设备管理 IP | 192.168.1.10 |
| 端口 | ✅ | 日志接收端口 | 514 |
| 协议 | ✅ | 日志传输协议 | Syslog |
| 日志格式 | ✅ | 日志格式规范 | CEF |
| 日志级别 | ✅ | 采集级别 | all |
| 传输方向 | ✅ | push(设备推送) / pull(平台拉取) | push |

3. 点击 **「测试连接」** 验证网络连通性
4. 测试通过后点击 **「保存」**

#### 4.13.3 数据源列表字段

| 字段 | 说明 |
|------|------|
| 名称 | 设备自定义名称 |
| 类型 | 设备类型图标 + 文字 |
| 品牌/型号 | 设备厂商信息 |
| 连接信息 | IP:Port |
| 协议 | Syslog / CEF / API |
| 状态 | 🟢 在线 / 🔴 离线 |
| 最后同步 | 最后收到日志的时间 |
| 日志量 | 日均日志量估算 |
| 健康度 | 0-100% 健康评分 |

#### 4.13.4 注意事项

- 新数据源保存到数据库的 `devices` 表中
- push 模式：设备主动向平台发送日志（需配置设备端的日志转发目标）
- pull 模式：平台定期从设备拉取日志（需配置 API 凭证）
- 建议为每个数据源设置合理的日志级别，避免全量采集导致性能问题

---

### 4.14 资产管理（/assets）

**功能定位**：企业安全资产的统一管理台账。

**入口位置**：侧边栏 → 平台管理 → 资产管理

#### 4.14.1 资产分类

| 分类 | 子类型 | 示例 |
|------|--------|------|
| 网络资产 | 路由器、交换机、防火墙、负载均衡 | Core-SW-01, FW-DMZ |
| 主机资产 | 服务器、工作站、虚拟机 | WEB-SVR01, WIN-02 |
| 应用资产 | Web 应用、API 服务、数据库 | OA 系统, MySQL-Primary |
| 云资产 | 云主机、对象存储、容器集群 | ECS-Prod, K8s-Cluster |
| 身份资产 | 域账号、服务账号、API Key | admin@secmind.com |

#### 4.14.2 资产属性

| 属性 | 说明 |
|------|------|
| 资产名称 | 唯一标识名 |
| 资产类型 | 分类和子类型 |
| IP 地址 / 域名 | 网络标识 |
| 所属业务线 | 业务归属 |
| 责任人 | 资产负责人 |
| 重要程度 | critical / high / medium / low |
| 脆弱性评分 | 基于 CVE 的风险评分 |
| 关联告警数 | 关联到此资产的历史告警 |

---

### 4.15 用户管理（/users）

**功能定位**：平台用户账户和权限的管理界面。

**入口位置**：侧边栏 → 平台管理 → 用户管理

#### 4.15.1 用户角色

| 角色 | 权限范围 | 适用人员 |
|------|----------|----------|
| 超级管理员 | 全部功能权限 | SOC 经理 / 安全负责人 |
| 管理员 | 除系统设置外的全部权限 | 高级安全工程师 |
| 分析师 | 运营总览 + 事件 lifecycle + AI 能力 | 安全分析师 |
| 只读用户 | 仅查看权限 | 审计员 / 管理层 |

#### 4.15.2 操作功能

- **创建用户**：填写用户名、邮箱、密码、角色
- **编辑用户**：修改角色、重置密码、启用/禁用
- **删除用户**：软删除，保留历史数据关联
- **批量操作**：批量导入（CSV）、批量角色变更
- **登录日志**：查看用户的登录历史和在线状态

---

### 4.16 报告中心（/reports）

**功能定位**：安全运营报告的生成、管理和导出。

**入口位置**：侧边栏 → 平台管理 → 报告中心

#### 4.16.1 报告类型

| 报告类型 | 频率 | 内容 |
|----------|------|------|
| 日报 | 每日自动 | 当日告警统计、处置概况、重大事件 |
| 周报 | 每周一自动 | 周度趋势、KPI 达标情况、TOP 事件 |
| 月报 | 每月1日自动 | 月度运营全景、同比环比、改进建议 |
| 事件报告 | 按需生成 | 单个案件的完整调查报告 |
| 态势报告 | 按需生成 | 特定主题的威胁态势分析 |
| 合规报告 | 按需生成 | 等保/合规要求的证明材料 |

#### 4.16.2 导出格式

- **PDF**：排版精美的正式报告，适合打印和存档
- **Word**：可编辑的文档格式
- **Excel**：数据和表格的原始格式
- **HTML**：在线浏览格式

---

### 4.17 审计日志（/audit）

**功能定位**：平台上所有操作的完整审计追踪。

**入口位置**：侧边栏 → 平台管理 → 审计日志

#### 4.17.1 日志字段

| 字段 | 说明 |
|------|------|
| 时间戳 | 操作发生的精确时间 |
| 操作人 | 执行操作的用户（或 AI引擎） |
| 操作类型 | create / read / update / delete / execute / approve |
| 操作对象 | 被操作的资源（告警/案件/用户/系统配置） |
| 操作详情 | 操作的具体内容（JSON 格式） |
| 来源 IP | 操作发起的 IP 地址 |
| 结果 | success / failure |
| 耗时 | 操作执行耗时（毫秒） |

#### 4.17.2 筛选与导出

- 按时间范围、操作人、操作类型、操作对象筛选
- 全文搜索操作详情内容
- 导出为 CSV（支持 10 万条以上大规模导出）
- 审计日志不可删除，保留期限遵循合规要求（默认 3 年）

---

### 4.18 集成中心（/integrations）

**功能定位**：第三方系统和服务的集成管理。

**入口位置**：侧边栏 → 平台管理 → 集成中心

#### 4.18.1 支持的集成类型

| 集成类型 | 说明 | 配置项 |
|----------|------|--------|
| 威胁情报 | VirusTotal / AlienVault OTX / 微步在线 | API Key |
| TI 平台 | MISP / Anomali ThreatStream | 连接地址 + API Key |
| 工单系统 | Jira / ServiceNow / 钉钉 | Webhook URL |
| 通知渠道 | 企业微信 / 钉钉 / Slack / Email | Bot Token |
| SIEM | Splunk / QRadar / ELK | API 连接配置 |
| 云平台 | AWS / Azure / 阿里云 | IAM 凭证 |

#### 4.18.2 Webhook 配置

每个集成支持配置 Webhook 回调：

```json
{
  "url": "https://your-webhook-endpoint/receive",
  "method": "POST",
  "headers": {
    "Authorization": "Bearer your-token",
    "Content-Type": "application/json"
  },
  "events": ["alert.created", "case.closed", "disposal.executed"],
  "enabled": true
}
```

**事件类型**：

| 事件 | 触发时机 |
|------|----------|
| alert.created | 新告警产生 |
| alert.confirmed | 告警被确认为真实攻击 |
| case.created | 新案件创建 |
| case.closed | 案件关闭 |
| disposal.executed | 处置动作执行完成 |
| disposal.failed | 处置动作执行失败 |
| feedback.submitted | 反馈提交 |

---

### 4.19 系统设置（/system）

**功能定位**：平台全局参数配置中心。

**入口位置**：侧边栏 → 平台管理 → 系统设置

#### 4.19.1 配置分类

| 分类 | 配置项 | 说明 |
|------|--------|------|
| **基本设置** | 平台名称、Logo、默认语言 | 外观相关 |
| **安全设置** | 密码策略、会话超时、登录锁定 | 安全策略 |
| **AI 配置** | 自动处置阈值、置信度阈值、审批规则 | AI 行为调优 |
| **通知设置** | 邮件服务器、通知模板、免打扰时段 | 通知渠道 |
| **数据保留** | 日志保留期、数据归档策略 | 存储管理 |
| **API 设置** | API Key 管理、速率限制、CORS | 开放接口 |
| **备份恢复** | 自动备份计划、备份存储位置 | 数据保护 |

#### 4.19.2 关键 AI 参数

| 参数 | 默认值 | 说明 | 调优建议 |
|------|--------|------|----------|
| 自动处置置信度阈值 | 85% | 超过此值自动执行处置 | 保守环境调至 90%+ |
| 高危告警置信度阈值 | 80% | 超此值为高危告警 | 根据误报率调整 |
| 人工审批阈值 | 78%-85% | 此区间内的处置需审批 | 降低则更自动化 |
| AI 学习批次大小 | 50 | 每 50 条反馈触发一次训练 | 数据量大可增大 |
| 推理链最大证据数 | 20 | 单阶段最多展示的证据条数 | 过多影响可读性 |

---

### 4.20 AI 调查助手（/ai-chat）

**功能定位**：对话式 AI 协作调查界面，支持自然语言驱动的安全分析。

**入口位置**：直接访问 `/ai-chat`（独立入口，不在侧边栏分组内）

#### 4.20.1 界面布局

```
┌────────────────┬──────────────────────────────────┐
│                │                                  │
│   左侧边栏      │        对话主区域                 │
│                │                                  │
│ • 新建对话按钮  │   ┌──────────────────────────┐   │
│ • 历史对话列表  │   │   PageHeader (标题栏)      │   │
│ • 数据范围信息  │   ├──────────────────────────┤   │
│                │   │                          │   │
│                │   │    消息滚动区域            │   │
│                │   │    (用户消息 + AI回复)     │   │
│                │   │                          │   │
│                │   │    快捷提问区域            │   │
│                │   │                          │   │
│                │   ├──────────────────────────┤   │
│                │   │    输入框 + 发送按钮       │   │
│                │   └──────────────────────────┘   │
└────────────────┴──────────────────────────────────┘
```

#### 4.20.2 左侧边栏功能

| 区域 | 内容 |
|------|------|
| **新建对话** | 清空当前对话，开始新的调查会话 |
| **历史对话** | 过去的对话记录列表，显示标题、时间、消息数 |
| **数据范围** | 当前 AI 可访问的数据范围（告警 30 天 / 威胁情报实时 / 资产全量 / 审计 90 天）|

#### 4.20.3 对话消息类型

AI 调查助手支持 6 种消息类型：

| 类型 | 触发条件 | 展示形式 |
|------|----------|----------|
| **text** | 一般问答 | 纯文本/Markdown 回复 |
| **evidence_list** | 涉及证据查询 | 结构化证据列表（来源/时间/详情/方向） |
| **tool_call** | AI 调用查询工具 | 可展开的工具调用卡片（输入参数 + 输出结果 + 耗时） |
| **confidence_update** | 涉及置信度调整 | 双进度条对比组件（调整前 → 调整后） |
| **suggestion** | AI 给出后续建议 | 可点击的建议芯片（Suggestion Chips） |
| **error** | 查询出错 | 错误提示信息 |

#### 4.20.4 快捷提问

首次打开或清空对话后，显示 6 个预设的快捷提问按钮：

| # | 图标 | 标签 | 预设查询 |
|---|------|------|----------|
| 1 | ShieldAlert | 分析最新告警 | 帮我分析一下最新的高危告警，给出攻击研判结论 |
| 2 | UserCheck | 账号行为分析 | zhangsan@secmind.com 这个账号最近有什么异常行为？ |
| 3 | Globe | IP 威胁查询 | 103.45.67.89 这个 IP 有什么威胁情报？ |
| 4 | FileSearch | 文件访问追溯 | 查询 WIN-02 设备过去 7 天的敏感文件访问记录 |
| 5 | Network | 横向移动检测 | 检测内网是否存在横向移动行为 |
| 6 | History | 今日安全态势 | 总结今天的安全态势，有哪些需要关注的事件？ |

#### 4.20.5 AI 调用的内部工具

当用户提问时，AI 调查助手会自动调用以下内部工具：

| 工具名称 | 功能 | 输入 | 输出示例 |
|----------|------|------|----------|
| query_auth_logs | 查询认证日志 | target, timeframe | 找到 N 条异常登录记录 |
| threat_intel_lookup | 威胁情报查询 | ioc, sources | 从 N 个情报源获取结果 |
| query_file_access | 文件访问查询 | target, timeframe, scope | 检索到 N 条访问记录 |
| lateral_movement_scan | 横向移动扫描 | scope, timeframe | 检测到 N 个可疑模式 |
| aggregate_security_posture | 安全态势聚合 | date, scope | 生成态势摘要 |

每个工具调用都以 **Tool Call 卡片** 形式展示，包含：
- 工具名称和图标
- 执行耗时
- 输入参数（JSON 格式，可展开）
- 返回结果文本

#### 4.20.6 操作要点

- **发送消息**：输入文字后按 `Enter`（或在手机端点击发送按钮）
- **换行**：按 `Shift + Enter` 在输入框中换行
- **复制回复**：点击 AI 回复下方的复制图标
- **评价质量**：点击 👍/👎 对 AI 回复质量进行反馈（影响后续学习）
- **选择建议**：点击建议芯片自动填入输入框并发送
- **新建对话**：点击左侧「新建对话」按钮
- **折叠侧边栏**：点击标题栏的折叠按钮扩大对话区域
- **关联案件**：点击输入框下方的「关联案件」按钮将对话关联到调查案件
- **附带附件**：点击「附带附件」上传文件供 AI 分析

#### 4.20.7 使用示例

**示例 1：分析 VPN 异常登录**

```
用户: 帮我分析一下最新的高危告警

AI: [工具调用] query_auth_logs(target="zhangsan@secmind.com", timeframe="7d")
    → 耗时 0.8s，找到 3 条异常登录记录

AI: 检测到以下异常点：
   ① VPN 异地登录 — 登录地从北京变为哈萨克斯坦
   ② 可疑 C2 通信 — 该 IP 被 APT28 标记
   ③ 时间线吻合 — 异常登录后 3 分钟内出现 PS 编码命令
   但发现矛盾证据：HR 系统显示该员工正在哈萨克斯坦出差。

   置信度: 92% → 45%（因出差申请反对证据下调）

[建议]: 联系员工确认 | 查看出差申请 | 对比历史基线
```

**示例 2：IP 威胁情报查询**

```
用户: 103.45.67.89 这个 IP 有什么威胁情报？

AI: [工具调用] threat_intel_lookup(ioc="103.45.67.89", sources=["virustotal","alienvault","internal"])
    → 耗时 1.2s，从 3 个情报源获取结果

AI: 威胁评分: 92/100 🔴 高危
   标签: C2节点、APT28、Tor出口
   关联组织: APT28 (Fancy Bear)
   内部关联: 该 IP 在过去 24h 内与 3 台内网设备通信

[建议]: 封禁该 IP | 隔离关联终端 | 创建狩猎假设
```

---

## 第五章 AI 功能使用指南

### 5.1 AI 研判引擎原理

#### 5.1.1 五阶段推理模型

SecMind 的 AI 研判引擎采用 **五阶段链式推理模型**，模拟资深安全分析师的思维过程：

```
┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│  事件发现  │ → │ 证据关联  │ → │ 模式匹配  │ → │ 置信度评估 │ → │ 处置决策  │
│ Phase 1  │   │ Phase 2  │   │ Phase 3  │   │ Phase 4  │   │ Phase 5  │
└──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
```

**Phase 1 — 事件发现（Event Discovery）**
- 从原始信号中提取关键事件要素
- 识别异常行为的时空特征
- 输出：「检测到了 X」

**Phase 2 — 证据关联（Evidence Correlation）**
- 跨数据源关联相关证据
- 时序对齐，建立因果关系
- 评估每条证据的方向（支持/反对/中性）
- 输出：「N 条证据支持，M 条反对」

**Phase 3 — 模式匹配（Pattern Matching）**
- 与已知攻击模式库比对
- 映射到 MITRE ATT&CK 技术 ID
- 关联外部威胁情报
- 输出：「匹配 TXXXX → TXXXX 攻击链」

**Phase 4 — 置信度评估（Confidence Assessment）**
- 贝叶斯推理加权计算
- 考虑证据时效性和来源可靠性
- 处理矛盾证据的降权
- 输出：「综合置信度 XX%」

**Phase 5 — 处置决策（Disposal Decision）**
- 基于置信度匹配处置策略
- 评估业务影响
- 确定是否需要人工审批
- 输出：「推荐执行 X 动作」

#### 5.1.2 证据方向体系

AI 在推理过程中对每条证据标注方向：

| 方向 | 含义 | 权重影响 |
|------|------|----------|
| **Supports（支持）** | 证据表明攻击假设可能成立 | + 增加置信度 |
| **Contradicts（反对）** | 证据表明攻击假设可能不成立 | - 降低置信度 |
| **Neutral（中性）** | 证据与假设无明显相关性 | 无直接影响 |

**特殊处理**：当存在强反对证据时，即使支持证据众多，AI 也会显著降低置信度并建议人工介入。例如：VPN 异地登录 + C2 通信等多条支持证据，但如果 HR 出差申请构成强反对证据，置信度可能从 92% 下调至 45%。

#### 5.1.3 多假设竞争机制

对于每个告警，AI 同时生成 **多个竞争假设**，而非单一结论：

```
告警: VPN 异地登录 + PowerShell 异常执行
    │
    ├── H-A: 账号被盗 (置信度 92%) ← 最可能的假设
    │     ├── 冻结账号 (pending)
    │     ├── 隔离主机 (executing)
    │     ├── 封禁 IP (completed)
    │     └── ...
    │
    ├── H-B: 内部人员威胁 (置信度 54%) ← 不能排除
    │     ├── 监控用户 (pending)
    │     └── 审查日志 (pending)
    │
    └── H-C: 误报 (置信度 12%) ← 可能性低
```

这种多假设机制避免了 AI 的"过度自信"，确保不会遗漏低概率但高风险的可能性。

### 5.2 AI 调查助手对话式操作

#### 5.2.1 设计理念

AI 调查助手采用 **对话式交互 + 工具调用** 的混合架构：

- **自然语言理解**：用户可以用日常语言描述调查需求
- **工具调用（Function Calling）**：AI 自动选择合适的内部工具查询数据
- **结构化输出**：返回证据列表、置信度评估、后续建议
- **连续对话**：支持多轮追问和逐步深入分析

#### 5.2.2 对话最佳实践

**✅ 推荐用法**：

| 做法 | 示例 | 效果 |
|------|------|------|
| 明确指定目标 | "分析 zhangsan 账号的异常登录" | AI 直接定位到目标 |
| 指定时间范围 | "查询过去 7 天的文件访问" | 缩小查询范围，结果更精确 |
| 组合多个问题 | "这个 IP 的威胁情报 + 关联的内网通信" | 一次性获得全面视图 |
| 基于建议继续 | 点击 AI 返回的建议芯片 | 沿着 AI 推荐的最优路径深入 |

**❌ 避免用法**：

| 做法 | 问题 |
|------|------|
| 过于宽泛的问题 | "帮我看看有什么安全问题" → 返回过多信息 |
| 缺少上下文 | "这个有问题吗？" → AI 不知道指什么 |
| 期望实时指令 | "现在帮我封禁这个 IP" → AI 调查助手仅做分析，不执行处置 |

#### 5.2.3 对话 vs 传统 UI 的选择

| 场景 | 推荐方式 | 原因 |
|------|----------|------|
| 快速了解某个 IOC | AI 调查助手 | 一句话搞定 |
| 深度调查一个案件 | 调查/案件页面 | 结构化的证据组织更好 |
| 批量处置操作 | 处置响应页面 | 可视化队列和审批流程 |
| 日常态势巡检 | 仪表盘 | KPI 和趋势一目了然 |
| 探索性分析 | AI 调查助手 | 自然语言灵活探索 |

### 5.3 反馈学习系统

#### 5.3.1 为什么需要反馈学习

AI 研判引擎虽然经过训练，但在面对特定企业的独特环境时可能出现偏差：

- **误报**：正常业务行为被误判为攻击（如合法的远程办公）
- **漏报**：新型攻击手法未被识别
- **置信度偏差**：判断方向正确但置信度过高或过低

反馈学习机制让每次人工纠正都成为 AI 进化的养料。

#### 5.3.2 反馈类型与影响

| 反馈类型 | 操作 | 对模型的影响 |
|----------|------|--------------|
| **确认（Confirmed）** | 点击「确认攻击」 | 提高此类攻击模式的特征权重 |
| **争议（Disputed）** | 点击「标记误报」+ 提供正确答案 | 降低误判模式的权重，学习正确模式 |
| **补充（Supplement）** | 添加额外的上下文信息 | 丰富决策依据，减少未来类似误判 |

#### 5.3.3 学习效果度量

在反馈学习的 **AI 成长曲线** 标签页中，可以观察：

- **总体准确率趋势**：应呈上升趋势
- **分攻击类型准确率**：某些类型可能有系统性偏差
- **里程碑事件**：如「首次突破 95%」「累计处理 1000 条反馈」
- **置信度校准曲线**：预测置信度与实际准确率的吻合程度

### 5.4 AI 自动处置策略

#### 5.4.1 置信度-优先级映射

AI 根据研判置信度自动确定处置动作的优先级：

| 置信度范围 | 优先级 | 自动化程度 | 说明 |
|------------|--------|------------|------|
| ≥ 80% | Critical | 高度自动 | 自动执行，异步通知 |
| 50% - 79% | High | 半自动 | 推荐执行，需快速审批 |
| 20% - 49% | Medium | 人工主导 | 仅建议，人工决定 |
| < 20% | Low | 仅记录 | 不触发处置，仅记录备查 |

#### 5.4.2 安全护栏机制

每个自动处置动作都配备 **四重安全护栏**：

| 护栏 | 内容 | 示例 |
|------|------|------|
| **审批护栏** | 是否需要人工审批及条件 | "自动阈值已满足，人工可在审计台一键撤销" |
| **影响护栏** | 预估的业务影响范围 | "影响 1 个账号，预计中断 VPN、SSO 与邮箱会话" |
| **回滚护栏** | 如何撤销已执行的处置 | "支持解冻账号、恢复 MFA 与重新下发临时凭证" |
| **审计护栏** | 完整的操作记录 | "记录 AI 推理链、触发策略、执行人与执行结果" |

#### 5.4.3 回滚机制

所有自动处置动作都支持回滚：

1. 在 **处置响应 → 执行记录** 标签页找到目标操作
2. 点击 **「回滚」** 按钮
3. 填写回滚原因（必填）
4. 系统执行反向操作并记录回滚日志
5. 回滚操作本身也会被审计记录

> **注意**：回滚操作需要授权角色才能执行，且回滚原因会被记录用于后续 AI 学习。

---

## 第六章 集成与配置

### 6.1 数据源接入配置

#### 6.1.1 Syslog 接入（最常用）

适用于防火墙、WAF、IDS/IPS、NAC 等支持 Syslog 协议的设备。

**设备端配置**（以 FortiGate 为例）：

```
config log syslogd setting
    status enable
    server "<SecMind-Server-IP>"
    port 514
    format cef
    facility local7
    source-ip "<FortiGate-Mgmt-IP>"
end
```

**平台端配置**：
1. 进入 **数据源** 页面
2. 点击「+ 添加数据源」
3. 类型选择对应设备类型（如「防火墙」）
4. 协议选择 `Syslog`
5. 端口填写 `514`（或自定义端口）
6. 日志格式选择 `CEF`
7. 传输方向选择 `push`
8. 保存后平台开始在指定端口监听

**验证方法**：保存后在数据源列表中查看状态是否变为 🟢 在线，并检查「最后同步」时间是否更新。

#### 6.1.2 API 接入

适用于 EDR、SIEM、云平台等支持 API 拉取的数据源。

**配置步骤**：
1. 传输方向选择 `pull`
2. 填写 API 地址（如 `https://edr.example.com/api/v1/logs`）
3. 配置认证凭证（API Key / OAuth / 证书）
4. 设置拉取间隔（建议 60s - 300s）
5. 保存并测试连接

#### 6.1.3 Agent 接入

适用于需要深度端点可见性的 EDR 解决方案。

**部署步骤**：
1. 在 **数据源** 页面下载 Agent 安装包
2. 在目标终端上安装 Agent
3. Agent 自动注册并开始上报数据
4. 在平台中查看 Agent 在线状态和健康度

### 6.2 Webhook 配置

Webhook 用于将 SecMind 的事件实时推送到第三方系统。

#### 6.2.1 配置入口

**集成中心** → 选择目标集成 → 编辑 → Webhook 配置

#### 6.2.2 Webhook Payload 格式

当配置的事件触发时，SecMind 会向目标 URL 发送 POST 请求：

```json
{
  "event_type": "alert.confirmed",
  "event_id": "evt_abc123",
  "timestamp": "2026-05-09T08:30:00Z",
  "data": {
    "alert_id": "ALT-20260509-0042",
    "title": "主机 WIN-02 检测到 PowerShell 编码执行",
    "severity": "critical",
    "confidence": 92,
    "hypothesis": "账号被盗",
    "assigned_to": "张明远"
  }
}
```

#### 6.2.3 签名验证

为确保 Webhook 请求的真实性，每个请求头中都包含 `X-SecMind-Signature` 签名：

```
X-SecMind-Signature: sha256=hex(hmac_sha256(webhook_secret, body))
```

接收方应验证签名以确保请求未被篡改。

### 6.3 SOAR 剧本编排

#### 6.3.1 创建自定义 Playbook

1. 进入 **自动化编排** 页面
2. 点击 **「+ 新建剧本」**
3. 在可视化编辑器中：
   - 从左侧节点面板拖拽节点到画布
   - 配置每个节点的参数
   - 用连线工具连接节点定义执行顺序
4. 配置触发条件
5. 点击 **「测试运行」** 在沙箱中验证
6. 测试通过后点击 **「发布」**

#### 6.3.2 节点参数配置示例

**触发节点（Trigger）**：
```yaml
type: alert_created
filters:
  severity: [critical, high]
  confidence:
    gte: 80
```

**条件节点（Condition）**：
```yaml
expression: "{{ confidence }} >= 85 && {{ impact_level }} != 'critical_business'"
branches:
  - label: "影响可控"
    target: action_freeze
  - label: "影响较大"
    target: approval_human
```

**动作节点（Action）**：
```yaml
type: freeze_account
target: "{{ detected_account }}"
params:
  reason: "AI auto-disposal, confidence={{ confidence }}%"
  notify: true
```

**审批节点（Approval）**：
```yaml
approvers: ["security_team_lead"]
timeout: 30m
escalation: security_manager
on_timeout: auto_approve  # or auto_reject
```

#### 6.3.3 剧本最佳实践

| 实践 | 说明 |
|------|------|
| 幂等性设计 | 确保重复执行不会产生副作用 |
| 超时保护 | 每个节点设置合理的超时时间 |
| 错误处理 | 为每个分支定义失败时的回退动作 |
| 版本管理 | 重大修改前先 Clone 保存旧版本 |
| 渐进式上线 | 先用 manual 触发模式测试，再改为自动 |

---

## 第七章 管理与运维

### 7.1 用户与权限管理

#### 7.1.1 角色权限矩阵

| 功能模块 | 超级管理员 | 管理员 | 分析师 | 只读用户 |
|----------|:---------:|:------:|:------:|:--------:|
| AI 分析工作台 | ✅ | ✅ | ✅ | 👁 |
| 安全大屏 | ✅ | ✅ | ✅ | ✅ |
| 运营仪表盘 | ✅ | ✅ | ✅ | 👁 |
| 信号流 | ✅ | ✅ | ✅ | 👁 |
| 调查/案件 | ✅ | ✅ | ✅ | 👁 |
| 处置响应 | ✅ | ✅ | ✅（仅查看） | ❌ |
| 反馈学习 | ✅ | ✅ | ✅ | ❌ |
| 威胁狩猎 | ✅ | ✅ | ✅ | 👁 |
| 自动化编排 | ✅ | ✅ | ❌ | ❌ |
| 数据源管理 | ✅ | ✅ | ❌ | ❌ |
| 用户管理 | ✅ | ❌ | ❌ | ❌ |
| 系统设置 | ✅ | ❌ | ❌ | ❌ |
| AI 调查助手 | ✅ | ✅ | ✅ | ✅ |

*图例：✅ 完全访问 / 👁 只读 / ❌ 无权访问*

#### 7.1.2 密码策略

| 策略项 | 默认值 | 说明 |
|--------|--------|------|
| 最小长度 | 8 字符 | |
| 复杂度要求 | 大写+小写+数字 | |
| 历史禁止 | 不能用最近 5 次密码 | |
| 有效期 | 90 天 | 到期强制修改 |
| 锁定策略 | 5 次失败锁定 30 分钟 | |
| 会话超时 | 8 小时无操作自动登出 | |

### 7.2 审计日志查看

#### 7.2.1 高风险操作告警

以下操作会在审计日志中被标记为 **高风险** 并触发额外通知：

- 用户角色变更
- 处置策略修改
- AI 阈值参数调整
- 数据源删除
- 系统密钥/凭证变更
- 大规模数据导出

#### 7.2.2 审计日志检索技巧

```bash
# 查找某用户今天的所有操作
操作人="admin" AND 时间戳 >= today

# 查找所有处置执行记录
操作类型 = "execute"

# 查找失败的登录尝试
操作类型 = "auth" AND 结果 = "failure"

# 查找 AI 引擎的所有自动操作
操作人 = "AI引擎"
```

### 7.3 系统设置与维护

#### 7.3.1 日常维护检查项

| 频率 | 检查项 | 方法 |
|------|--------|------|
| 每日 | 数据源在线状态 | 数据源页面查看 🟢/🔴 状态 |
| 每日 | 告警积压量 | 仪表盘查看 pending 数量 |
| 每周 | AI 准确率趋势 | 反馈学习 → AI 成长曲线 |
| 每月 | 磁盘空间使用 | 系统设置 → 存储管理 |
| 每月 | 用户权限审查 | 用户管理 → 审查非活跃账号 |
| 每季度 | 备份完整性验证 | 系统设置 → 备份恢复 → 验证备份 |

#### 7.3.2 性能调优建议

| 场景 | 建议 |
|------|------|
| 告警量大（日均 > 5000） | 提高 AI 研判并行度，增加日志保留聚合 |
| 数据源多（> 20 个） | 按优先级分级采集，非关键源降低采样率 |
| 并发用户多（> 50） | 启用 Redis 缓存，增加 API 限流 |
| 历史数据大（> 1 年） | 启用数据归档，热数据保留 90 天 |

#### 7.3.3 备份与恢复

**自动备份配置**（建议）：
- **增量备份**：每日凌晨 3:00 执行
- **全量备份**：每周日凌晨 3:00 执行
- **备份保留**：增量保留 7 天，全量保留 4 周
- **备份存储**：异地存储（如 OSS / S3）

**恢复流程**：
1. 停止 SecMind 服务
2. 从备份恢复数据库文件
3. 验证数据完整性（检查记录数）
4. 重启服务
5. 验证核心功能可用

---

## 第八章 常见问题 FAQ

### Q1：AI 研判的置信度是什么意思？应该信任多高的置信度？

**A**：置信度是 AI 对"这是一个真实攻击"的概率估计，取值 0-100%。一般建议：
- **≥ 85%**：高度可信，可以基本依赖 AI 的判断
- **65%-84%**：中等可信，建议快速人工复核
- **< 65%**：低可信度，必须人工深入调查

置信度不是绝对准确的，它会随着反馈学习不断校准。初期建议对所有 ≥ 80% 的研判都进行人工复核。

### Q2：如何降低误报率？

**A**：降低误报率的关键在于**持续的反馈学习**：
1. 定期（建议每天）查看 **反馈学习** 页面的待反馈列表
2. 对每条误报点击「标记误报」并提供正确的分类
3. 特别关注反复出现的误报模式，考虑在 **知识库** 中添加例外规则
4. 在 **系统设置 → AI 配置** 中适当提高自动处置的置信度阈值
5. 经过 2-4 周的持续反馈，误报率通常会显著下降

### Q3：自动处置会不会造成业务中断？

**A**：平台设计了多重安全机制来防止这种情况：
1. **置信度门槛**：只有高置信度（默认 ≥ 85%）才触发自动处置
2. **影响评估**：每个处置动作执行前会评估业务影响
3. **人工审批**：中高风险动作需要人工批准
4. **即时回滚**：所有处置动作都可以在一键回滚
5. **渐进式上线**：建议先开启「通知模式」（只通知不执行），观察一周后再开启自动执行

### Q4：数据源显示离线怎么办？

**A**：按以下步骤排查：
1. **检查网络连通性**：从 SecMind 服务器 ping 数据源 IP
2. **检查端口**：`telnet <数据源 IP> <端口>` 确认端口可达
3. **检查防火墙规则**：确保 SecMind 服务器的入站规则允许数据源端口的流量
4. **检查设备端配置**：确认设备端的日志转发目标地址和端口正确
5. **检查协议匹配**：平台配置的协议（Syslog/CEF/API）必须与设备端一致
6. **查看平台日志**：`backend/logs/` 目录下可能有详细的连接错误信息

### Q5：AI 调查助手和 AI 分析工作台有什么区别？该用哪个？

**A**：两者的定位不同：

| 维度 | AI 调查助手 | AI 分析工作台 |
|------|-------------|--------------|
| **交互方式** | 对话式（自然语言） | 结构化界面（点击操作） |
| **适用场景** | 探索性分析、快速查询 | 深度研判、正式调查 |
| **输出形式** | 对话消息 + 证据列表 | 完整推理链 + 假设面板 |
| **处置能力** | 仅分析和建议 | 可直接触发处置 |
| **协作性** | 单人对话为主 | 适合团队协作办案 |

**简单原则**：想快速了解某个问题 → 用 AI 调查助手；要做正式的调查和处置 → 用 AI 分析工作台。

### Q6：如何添加新的 ATT&CK 战术/技术覆盖？

**A**：SecMind 的 ATT&CK 覆盖基于内置的战术技术库。如需扩展：
1. 在 **威胁狩猎** 页面创建假设时，可以选择任意已有的战术/技术
2. 如果需要的战术/技术不在列表中，可以通过 **系统设置 → 知识库管理** 添加自定义战术/技术定义
3. 添加后即可在狩猎假设中选择使用
4. 也支持通过 **集成中心 → TI 平面** 同步最新的 ATT&CK 数据

### Q7：反馈学习的效果多久能体现？

**A**：这取决于反馈的量和质量：
- **少量反馈（< 50 条）**：1-2 周内可以看到局部改善
- **中等反馈（50-200 条）**：2-4 周可以看到明显的准确率提升
- **大量反馈（200+ 条）**：1-2 个月可以达到稳定的高准确率（> 95%）

关键是反馈的**质量**比数量更重要——每条反馈都应该经过仔细核实。可以在 **反馈学习 → AI 成长曲线** 中实时跟踪学习效果。

### Q8：支持多租户/多部门隔离吗？

**A**：当前版本基于用户角色进行权限控制。多租户隔离可通过以下方式实现：
1. 利用 **用户管理** 中的角色体系，为不同部门创建不同的角色
2. 利用 **资产管理** 中的「所属业务线」字段进行逻辑隔离
3. 在 **数据源** 配置时按部门分配不同的数据源
4. 后续版本将原生支持多租户数据隔离

### Q9：如何导出调查报告？

**A**：有两种方式：
1. **从调查/案件页面**：打开案件详情 → 点击「导出报告」→ 选择格式（PDF/Word/Excel）
2. **从报告中心**：选择「事件报告」类型 → 选择案件 → 生成报告

导出的报告包含：案件信息、时间线、证据清单、AI 研判结论、处置记录、调查结论。

### Q10：平台支持哪些语言的 API？

**A**：SecMind 后端基于 FastAPI，提供 RESTful API，支持任何编程语言的 HTTP 调用。主要接口包括：
- `GET /api/alerts` — 获取告警列表
- `POST /api/alerts/{id}/analyze` — 触发 AI 研判
- `GET /api/cases` — 获取案件列表
- `POST /api/disposal/execute` — 执行处置动作
- `POST /api/ai-chat` — AI 调查助手接口

完整的 API 文档可通过访问 `http://your-backend:8000/docs` 查看（Swagger UI）。

---

## 附录 A：术语表

| 术语 | 英文 | 定义 |
|------|------|------|
| **SOC** | Security Operations Center | 安全运营中心 |
| **SIEM** | Security Information & Event Management | 安全信息和事件管理 |
| **SOAR** | Security Orchestration, Automation and Response | 安全编排、自动化与响应 |
| **EDR** | Endpoint Detection and Response | 端点检测与响应 |
| **IOC** | Indicator of Compromise | 失陷指标（IP、域名、Hash 等） |
| **TTP** | Tactics, Techniques, Procedures | 战术、技术和过程 |
| **ATT&CK** | Adversary Tactics, Techniques & Common Knowledge | MITRE 攻击知识库 |
| **MITRE ATT&CK** | — | MITRE 维护的攻击战术和技术框架 |
| **MTTD** | Mean Time To Detect | 平均检测时间 |
| **MTTR** | Mean Time To Respond | 平均响应时间 |
| **MTTA** | Mean Time To Analyze | 平均分析时间 |
| **C2** | Command and Control | 命令与控制（攻击者远程控制信道） |
| **DGA** | Domain Generation Algorithm | 域名生成算法 |
| **APT** | Advanced Persistent Threat | 高级持续性威胁 |
| **CEF** | Common Event Format | 通用事件格式（日志标准） |
| **Syslog** | System Logging | 系统日志协议 |
| **WebShell** | — | Web 后门程序 |
| **Playbook** | — | SOAR 自动化剧本（响应预案） |
| **假设置信度** | Hypothesis Confidence | AI 对某个攻击假设成立概率的估计 |
| **推理链** | Reasoning Chain | AI 从证据到结论的完整推导过程 |
| **安全护栏** | Guardrails | 自动处置的安全约束机制 |
| **反馈学习** | Feedback Learning | 基于人工反馈优化 AI 模型的机制 |
| **威胁狩猎** | Threat Hunting | 主动搜索潜在威胁的安全实践 |
| **信号** | Signal | 原始安全事件数据（未经 AI 分析） |
| **告警** | Alert | 经 AI 筛选/升级后的安全警报 |
| **案件** | Case / Incident | 需要深入调查的安全事件 |
| **处置** | Disposal / Response | 对安全事件采取的抑制/消除措施 |
| **横向移动** | Lateral Movement | 攻击者在内网中从一台设备转移到另一台 |
| **凭证窃取** | Credential Access | 攻击者获取合法用户的认证凭据 |
| **持久化** | Persistence | 攻击者在系统中维持长期访问的能力 |
| **SOP** | Standard Operating Procedure | 标准操作规程 |
| **SLA** | Service Level Agreement | 服务水平协议 |
| **DLP** | Data Loss Prevention | 数据防泄漏 |
| **NAC** | Network Access Control | 网络准入控制 |
| **WAF** | Web Application Firewall | Web 应用防火墙 |
| **RDP** | Remote Desktop Protocol | 远程桌面协议 |
| **SMB** | Server Message Block | 服务器消息块（文件共享协议） |
| **VPN** | Virtual Private Network | 虚拟专用网络 |
| **AD** | Active Directory | 活动目录（微软域控） |
| **Kerberos** | — | 网络认证协议 |
| **MFA** | Multi-Factor Authentication | 多因素认证 |
| **SSO** | Single Sign-On | 单点登录 |
| **JWT** | JSON Web Token | JSON Web 令牌（认证凭证格式） |
| **Mimikatz** | — | Windows 凭证提取工具（常被攻击者使用） |
| **Cobalt Strike** | — | 商业化渗透测试框架（常被 APT 组织使用） |
| **PowerShell** | — | Windows 命令行 shell 和脚本环境（常被滥用） |
| **鱼叉式钓鱼** | Spear Phishing | 针对特定目标的定向钓鱼攻击 |
| **Tor** | — | 匿名通信网络（常被用作 C2 通道） |
| **STIX** | Structured Threat Information Expression | 结构化威胁信息表达式（威胁情报交换格式） |

---

## 附录 B：快捷键参考

### 全局快捷键

| 快捷键 | 功能 | 适用范围 |
|--------|------|----------|
| `?` | 打开键盘快捷键帮助 | 全局 |
| `F11` | 全屏切换 | 安全大屏 |
| `Esc` | 关闭弹窗/对话框 | 全局 |
| `/` | 聚焦全局搜索框 | 后台页面 |

### AI 调查助手快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 发送消息 |
| `Shift + Enter` | 输入框内换行 |
| `↑ / ↓` | 浏览历史对话（输入框聚焦时） |
| `Ctrl + Shift + N` | 新建对话 |

### 信号流/告警列表快捷键

| 快捷键 | 功能 |
|--------|------|
| `j / k` | 上/下移动选择 |
| `o` | 打开选中项详情 |
| `e` | 升级为告警（信号流） |
| `/` | 聚焦搜索框 |
| `f` | 打开筛选器 |

### 处置响应快捷键

| 快捷键 | 功能 |
|--------|------|
| `j / k` | 上/下移动选择动作 |
| `Enter` | 执行/批准选中动作 |
| `r` | 展开推理链 |
| `Tab` | 切换标签页 |

---

## 附录 C：错误码速查表

### 认证相关 (1xxx)

| 错误码 | HTTP 状态 | 含义 | 解决方法 |
|--------|-----------|------|----------|
| 1001 | 401 | 用户名或密码错误 | 检查用户名和密码，注意大小写 |
| 1002 | 401 | Token 已过期 | 重新登录获取新 Token |
| 1003 | 403 | 权限不足 | 联系管理员提升权限 |
| 1004 | 400 | 账号已被锁定 | 等待 30 分钟后重试或联系管理员 |
| 1005 | 409 | 用户名已存在 | 使用其他用户名注册 |
| 1006 | 400 | 密码不符合策略 | 确保密码 ≥ 8 位且包含大小写和数字 |
| 1010 | 401 | SSO 认证失败 | 检查 IdP 配置和回调 URL |
| 1011 | 400 | Token 格式无效 | 清除浏览器缓存重新登录 |

### AI 研判相关 (2xxx)

| 错误码 | HTTP 状态 | 含义 | 解决方法 |
|--------|-----------|------|----------|
| 2001 | 500 | AI 引擎不可用 | 检查后端服务状态，重启 AI 服务 |
| 2002 | 400 | 告警数据不完整 | 检查告警必填字段 |
| 2003 | 429 | AI 请求过于频繁 | 降低请求频率，稍后重试 |
| 2004 | 503 | AI 模型加载中 | 等待 1-2 分钟后重试 |
| 2005 | 400 | 证据不足无法研判 | 补充更多关联数据后重试 |
| 2010 | 500 | 推理链生成超时 | 减少单次分析的证据数量 |
| 2011 | 400 | 置信度超出有效范围 | 置信度必须在 0-100 之间 |

### 处置相关 (3xxx)

| 错误码 | HTTP 状态 | 含义 | 解决方法 |
|--------|-----------|------|----------|
| 3001 | 400 | 处置动作不存在 | 检查动作 ID 是否正确 |
| 3002 | 403 | 无执行权限 | 联系管理员获取处置权限 |
| 3003 | 409 | 动作已在执行中 | 不要重复执行，等待完成 |
| 3004 | 500 | 目标设备不可达 | 检查目标设备的网络连通性 |
| 3005 | 400 | 置信度未达阈值 | 该动作需要更高置信度才可执行 |
| 3006 | 403 | 需要审批 | 先完成审批流程 |
| 3010 | 500 | 回滚失败 | 手动执行回滚操作或联系管理员 |
| 3011 | 400 | 超过回滚时间窗口 | 部分动作仅在 24h 内可回滚 |

### 数据源相关 (4xxx)

| 错误码 | HTTP 状态 | 含义 | 解决方法 |
|--------|-----------|------|----------|
| 4001 | 400 | 连接参数不完整 | 检查 IP、端口、协议等必填字段 |
| 4002 | 500 | 连接测试失败 | 检查网络和防火墙规则 |
| 4003 | 409 | 数据源名称已存在 | 使用唯一名称 |
| 4004 | 404 | 数据源不存在 | 检查数据源 ID |
| 4005 | 500 | 日志解析失败 | 检查日志格式配置是否与设备输出匹配 |
| 4010 | 500 | 数据源同步超时 | 检查网络带宽和设备负载 |

### 系统相关 (5xxx)

| 错误码 | HTTP 状态 | 含义 | 解决方法 |
|--------|-----------|------|----------|
| 5001 | 500 | 数据库连接失败 | 检查数据库服务状态 |
| 5002 | 500 | 磁盘空间不足 | 清理日志或扩容磁盘 |
| 5003 | 500 | 内部服务器错误 | 查看后端日志，联系技术支持 |
| 5004 | 408 | 请求超时 | 稍后重试或减少请求数据量 |
| 5010 | 403 | API Rate Limit | 降低请求频率 |
| 5011 | 400 | 请求格式错误 | 检查请求体 JSON 格式 |
| 5012 | 405 | 方法不允许 | 检查 HTTP 方法（GET/POST/PUT/DELETE） |

---

> **文档结束**
>
> © 2026 SecMind. All Rights Reserved.
> 如有疑问或建议，请联系：support@secmind.com
